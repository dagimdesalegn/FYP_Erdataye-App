# Web framework
fastapi>=0.115.0
uvicorn[standard]>=0.32.0

# AI / LLM
openai>=2.32.0

# HTTP client (async, connection-pooled)
httpx>=0.27.0

# JWT verification (Supabase tokens)
PyJWT>=2.9.0
cryptography>=42.0.0

# Settings from environment variables
pydantic-settings>=2.4.0
pydantic>=2.8.0

# Env file support
python-dotenv>=1.0.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0

# Error tracking (optional – activate by setting SENTRY_DSN env var)
sentry-sdk[fastapi]>=2.0.0
